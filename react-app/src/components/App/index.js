import React, { Fragment } from 'react';
import Logo from '../Logo';
import GlobalStyle from './GlobalStyle';

const App = () => (
  <Fragment>
    <Logo />
    <GlobalStyle />
  </Fragment>
);

export default App;
